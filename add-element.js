(function greeting(){ //instantly invoked function to show the greeting on the page and set time and dayvar date = new Date(); //gets datevar todayDate, month, hour, mins; //sets variables for month hour minstodayDate = date.getDate();month = date.getMonth(); //gets monthhour = date.getHours(); //gets hourmins = date.getMinutes(); //gets minutesday = date.getDay();console.log(month, hour, mins); //testing that data is comeing throughdayName = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday']; //not usedmonthNames = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];currentMonth = monthNames[month]; //sets the month as a stringcurrentDay = dayName[day];console.log(currentMonth);var morningGreeting = 'Good morning';var dayGreeting = 'Good day';var nightGreeting = 'Good evening';var elGreeting = document.getElementById('welcomeGreeting');if (hour < 12 && hour > 1){  elGreeting.textContent = morningGreeting;}else if (hour > 12 && hour < 17){  elGreeting.textContent = dayGreeting;}else {  elGreeting.textContent = nightGreeting;}var elDay = document.getElementById('day');var elMonth = document.getElementById('month');var elDate = document.getElementById('date');elDay.innerHTML = currentDay + ',';elMonth.textContent = currentMonth;elDate.textContent = todayDate;})(); //finishes the function to show the date on the pagefunction startShopping(){//event to show message on Welcome Page  var elFeedback = document.getElementById('feedback');  elFeedback.innerHTML = "<p>Great let's get started!</p>";}function mouseOffStart(){//event to hide welcome message on home page  var elFeedback = document.getElementById('feedback');  elFeedback.innerHTML = "";}var elStart = document.getElementById('start');elStart.addEventListener('mouseover', startShopping, false);elStart.addEventListener('mouseout', mouseOffStart, false);//function to create new items for salefunction addNewItem(name, price, image){  this.name = name;  this.price = price;  this.image = image;}var figs = new addNewItem("figs", 5, "<img src='fig.jpg' class='fig'>");console.log(figs);// Create a new element and store it in a variable.var newEl = document.createElement('li');// Create a text node and store it in a variable.var newText = document.createTextNode(figs);// Attach the new text node to the new element.newEl.appendChild(newText);// Find the position where the new element should be added.var position = document.getElementsByTagName('ul')[0];// Insert the new element into its position.position.appendChild(newEl);
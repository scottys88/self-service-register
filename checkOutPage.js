//function to create new items for salefunction addNewItem(name, price, image){  this.name = name;  this.price = price;}var figs = new addNewItem("figs", 5);var pine_nuts = new addNewItem("pine_nuts", 3);var honey = new addNewItem("honey", 4);var balsamic = new addNewItem("balsamic", 10);var elAddCart = document.getElementsByClassName('addCart');var totalButton = document.getElementById('addItems');var grandTotal = 0;function addToCart(){  for (i = 0; i < elAddCart.length; i++){    qty = elAddCart[i].previousSibling.value;    console.log(elAddCart[i].parentNode.id);      if (qty > 0){    var form = document.getElementsByName('itemsForSale')[i];    item = elAddCart[i].parentNode.id;      switch (item){        case figs.name:          price = qty * figs.price;            var newEl = document.createElement('li');            var newText = document.createTextNode(qty + " x " + figs.name + ' $' + price);            newEl.appendChild(newText);            var remove = document.createElement('b');            remove.innerHTML = '<input type=\'button\' class=\'remove\' value=\'remove\'>';            newEl.appendChild(remove);            var position = document.getElementById('order');            var children = position.children.length + i;            position.appendChild(newEl);            elAddCart[i].previousSibling.value = "";            grandTotal += price;            console.log(grandTotal)            console.log(remove.parentNode);          break;        case pine_nuts.name:          price = qty * pine_nuts.price;            var newEl = document.createElement('li');            var newText = document.createTextNode(qty + " x " + pine_nuts.name + ' $' + price);            newEl.appendChild(newText);            var remove = document.createElement('b');            remove.innerHTML = '<span>remove</span>';            newEl.appendChild(remove);            var position = document.getElementById('order');            var children = position.children.length + i;            position.appendChild(newEl);            elAddCart[i].previousSibling.value = "";            grandTotal += price;            console.log(grandTotal)          break;        case honey.name:          price = qty * honey.price;            var newEl = document.createElement('li');            var newText = document.createTextNode(qty + " x " + honey.name + ' $' + price);            newEl.appendChild(newText);            var remove = document.createElement('b');            remove.innerHTML = '<span>remove</span>';            newEl.appendChild(remove);            var position = document.getElementById('order');            var children = position.children.length + i;            position.appendChild(newEl);            elAddCart[i].previousSibling.value = "";            grandTotal += price;            console.log(grandTotal)          break;        case balsamic.name:          price = qty * balsamic.price;            var newEl = document.createElement('li');            var newText = document.createTextNode(qty + " x " + balsamic.name + ' $' + price);            newEl.appendChild(newText);            var remove = document.createElement('b');            remove.innerHTML = '<span>remove</span>';            newEl.appendChild(remove);            var position = document.getElementById('order');            var children = position.children.length + i;            position.appendChild(newEl);            elAddCart[i].previousSibling.value = "";            grandTotal += price;            console.log(grandTotal);          break;        }      }    }}function showTotal(){            var elTotal = document.getElementById('total');            elTotal.innerHTML = "$"+grandTotal;}function findItem(){      var orderList = document.getElementById('order');      console.log(orderList.length);      for (j = 1; j < orderList.length; j++){      }}function update(){  console.log("eat shit");}for (i = 0; i<elAddCart.length; i++){  elAddCart[i].addEventListener('click', function(){addToCart(); showTotal();}, false);}totalButton.addEventListener('click', function(){addToCart(); showTotal();}, false);var orderList = document.getElementById('order');orderList.addEventListener('DOMNodeInserted', findItem, false);// // Create a new element and store it in a variable.// var newEl = document.createElement('li');////// // Create a text node and store it in a variable.// var newText = document.createTextNode(figs.name + ", " + figs.price + " /kg, " + "Enter weight " + "<input type='button' value='add to cart'>");// var newButton = document.createElement("Button");// var buttonText = document.createTextNode(cartCopy);// newButton.addEventListener('click', addToCart, false);// newButton.appendChild(buttonText);//// // Attach the new text node to the new element.// newEl.appendChild(newText);// newEl.appendChild(newButton);//// // Find the position where the new element should be added.// var position = document.getElementsByTagName('ul')[0];//// // Insert the new element into its position.// position.appendChild(newEl);